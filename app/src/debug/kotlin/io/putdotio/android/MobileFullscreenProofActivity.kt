package io.putdotio.android

import androidx.activity.ComponentActivity

/** Hosts local video proof while the real window rotates; production activity recreation remains unchanged. */
class MobileFullscreenProofActivity : ComponentActivity()
