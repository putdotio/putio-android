<?xml version="1.0" encoding="utf-8"?>
<!-- Only exported copies are shareable; the download cache and everything else stay private. -->
<paths>
    <files-path name="shares" path="shares/" />
</paths>
